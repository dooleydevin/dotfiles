# Core CLI utilities
brew "curl"
brew "wget"
brew "ripgrep"
brew "fd"
brew "jq"
brew "htop"

# Zsh plugins (symlinked into oh-my-zsh-custom by chezmoi script)
brew "powerlevel10k"
brew "zsh-vi-mode"

# macOS-specific: GNU coreutils, sed, grep
if OS.mac?
  brew "coreutils"
  brew "gnu-sed"
  brew "grep"
end
